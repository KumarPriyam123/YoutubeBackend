# A Basic Youtube backend
YouTube-style backend system that supports user authentication, video management, subscriptions, and watch history tracking using MongoDB aggregation pipelines. The backend exposes RESTful APIs consumed by a React frontend.
